.BEGIN EXPORT
  SESSIONS 9;

.EXPORT OUTFILE ../../INTERCAMBIOS/TMP/procesos_gde.txt
  FORMAT TEXT MODE RECORD;


SELECT CAST(TRIM(proceso) AS CHAR(30)) 
FROM &bd_ctl&.CTL_PROCESOS_CARGA
WHERE 
  fase_carga='FASE_GDE'  
ORDER BY 
	orden_proceso;

.END EXPORT;

.LOGOFF;

